#include "main.h"

/**
*_isupper - check for sup_case char
*@c: is the char need to be checked
*
*Return: 1 for uppercase or 0 otherwise
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
	_putchar('\n')
}
